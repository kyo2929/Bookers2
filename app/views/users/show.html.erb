<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>

<% @book.each do |book| %>
<div>
  <%= link_to book_path(book.id) %>
    <%= image_tag book.get_image %>
  <% end %>
  
</div>